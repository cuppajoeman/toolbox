" the following command makes it so that whenever there is a .vimrc in the
" directory where vim is launched it will stack that configuration on top of
" the current ~/.vimrc settings. This is beneficial because it allows you to
" have project specific settings that can run on top of your base config.
" check the help for exrc for more information
set exrc
